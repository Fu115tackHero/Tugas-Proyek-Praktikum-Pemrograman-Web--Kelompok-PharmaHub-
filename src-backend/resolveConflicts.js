#!/usr/bin/env node
/*
  Moved helper script for conflict resolution into src-backend/
  (kept original content for reference)
*/

const fs = require("fs");
const path = require("path");

console.log("resolveConflicts helper moved to src-backend (no-op)");
